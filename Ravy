ravy/
├── index.html        ← Login / Registro
├── app.html          ← RAVY (solo usuarios logueados)
├── firebase.js       ← Configuración Firebase
├── auth.js           ← Lógica login / registro
└── ravy.html         ← (opcional si separas escenas)
